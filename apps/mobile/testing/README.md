# mobile-testing

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build mobile-testing` to build the library.

## Running unit tests

Run `nx test mobile-testing` to execute the unit tests via [Jest](https://jestjs.io).
