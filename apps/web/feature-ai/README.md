# web-feature-ai

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build web-feature-ai` to build the library.

## Running unit tests

Run `nx test web-feature-ai` to execute the unit tests via [Vitest](https://vitest.dev/).
