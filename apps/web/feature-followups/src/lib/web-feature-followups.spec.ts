import { webFeatureFollowups } from './web-feature-followups';

describe('webFeatureFollowups', () => {
  it('should work', () => {
    expect(webFeatureFollowups()).toEqual('web-feature-followups');
  });
});
