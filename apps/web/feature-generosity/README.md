# web-feature-generosity

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build web-feature-generosity` to build the library.

## Running unit tests

Run `nx test web-feature-generosity` to execute the unit tests via [Vitest](https://vitest.dev/).
