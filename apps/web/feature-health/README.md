# web-feature-health

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build web-feature-health` to build the library.

## Running unit tests

Run `nx test web-feature-health` to execute the unit tests via [Vitest](https://vitest.dev/).
