# feature-interactions

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build feature-interactions` to build the library.

## Running unit tests

Run `nx test feature-interactions` to execute the unit tests via [Vitest](https://vitest.dev/).
