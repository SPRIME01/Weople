import { featureInteractions } from './feature-interactions';

describe('featureInteractions', () => {
  it('should work', () => {
    expect(featureInteractions()).toEqual('feature-interactions');
  });
});
