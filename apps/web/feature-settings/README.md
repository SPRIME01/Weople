# web-feature-settings

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build web-feature-settings` to build the library.

## Running unit tests

Run `nx test web-feature-settings` to execute the unit tests via [Vitest](https://vitest.dev/).
