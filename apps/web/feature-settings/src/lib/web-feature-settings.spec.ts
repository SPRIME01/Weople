import { webFeatureSettings } from './web-feature-settings';

describe('webFeatureSettings', () => {
  it('should work', () => {
    expect(webFeatureSettings()).toEqual('web-feature-settings');
  });
});
