# web-feature-tags

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build web-feature-tags` to build the library.

## Running unit tests

Run `nx test web-feature-tags` to execute the unit tests via [Vitest](https://vitest.dev/).
