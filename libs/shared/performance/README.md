# performance

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build performance` to build the library.

## Running unit tests

Run `nx test performance` to execute the unit tests via [Vitest](https://vitest.dev/).
