# feature-contacts

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build feature-contacts` to build the library.

## Running unit tests

Run `nx test feature-contacts` to execute the unit tests via [Vitest](https://vitest.dev/).
